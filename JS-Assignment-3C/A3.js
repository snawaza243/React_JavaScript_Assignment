/**
 * What are synthetic events in react?
 * the wrapper that react sues to standardize event functionality across browsers, they are very similar to native events like onClick(), onBlur(), onChange()
 */